import Image from 'next/image'
import { Callout, Steps, Tabs, Cards, Card } from 'nextra/components'
import { FaIcons } from "react-icons/fa6";
import { HiOutlineRocketLaunch } from "react-icons/hi2";
import { FaBoxOpen } from "react-icons/fa";


# Documentation Site

The website you are currently viewing is built using [NEXTRA](https://nextra.site/), a framework built on top of Next.js. It offers both Documentation and Blog themes, and this instance employs the [Documentation Theme](https://nextra.site/docs).

<Callout>
  The easiest way to install and deploy this site is by pressing the Deploy button below. The template project will be forked into your GitHub repository and automatically deployed on [Vercel](https://vercel.com/).
  <a href="https://vercel.com/new/clone?s=https%3A%2F%2Fgithub.com%2Fshuding%2Fnextra-docs-template&showOptionalTeamCreation=false">
  <Image src="https://vercel.com/button" alt="Deplay button" width="100" height="10"/>
  </a> 
</Callout>
However, please note that this site has been installed as a new project and deployed on GitHub Pages. For How to deploy Next.js Static App on GitHub Pages, please click on the following button.
<Cards>
  <Card icon={<FaBoxOpen size="22"/>} title="Deploy on GitHub Pages" href="/nextjs-to-github-pages" />
</Cards>
Later, to utilize icons on this site, React-icons has been installed.
<Cards>
  <Card icon={<FaIcons size="20"/>} title="Install Icons" href="/icons#icons-for-this-site" />
</Cards>


## Running Locally

<Tabs items={['Using Template', 'Starting a New Project']} margin-left="20">
  <Tabs.Tab>
    <Steps>
      ### Creating the Project
      Click on the following deploy button to create a project from a template and deploy it on Versel. 
      <a href="https://vercel.com/new/clone?s=https%3A%2F%2Fgithub.com%2Fshuding%2Fnextra-docs-template&showOptionalTeamCreation=false">
        <Image src="https://vercel.com/button" alt="Deplay button" width="100" height="10"/>
      </a>

    ### Editing .gitignore
    Replace your current `.gitignore` with the following.
    <details>
    <summary> Click here to view the new `.gitignore` code.</summary>
    
        ``` ansi filename=".gitignore" copy
        # See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

        # dependencies
        /node_modules
        /.pnp
        .pnp.js
        .yarn/install-state.gz

        # testing
        /coverage

        # next.js
        /.next/
        /out/

        # production
        /build

        # misc
        .DS_Store
        *.pem

        # debug
        npm-debug.log*
        yarn-debug.log*
        yarn-error.log*

        # local env files
        .env*.local

        # vercel
        .vercel

        # typescript
        *.tsbuildinfo
        next-env.d.ts
        ```
    </details>

     ### Installation
    The site is already running on Vercel. However, to run the site on your computer, navigate to the project directory in the terminal and execute `npm i` to install the necessary node modules.

     ```bash copy
        npm i
    ```

    ### Running the Site
    Execute `npm run dev` and check if the site is running in your browser at(http://localhost:3000).

     ```bash copy
        npm run dev
    ```

    </Steps>
  </Tabs.Tab>
  <Tabs.Tab>
    <Steps>
    ### Create Repository in GitHub
    First, create a new repository with README.md, LICENSE (MIT), and .gitignore (node), then pull it to your computer.
    ### Installation
      Navigate to the project directory in the terminal and follow the [instruction](https://nextra.site/docs/docs-theme/start#start-as-new-project) in NEXTRA to install the necessary libraries and run the project locally.
    </Steps>
  </Tabs.Tab>
</Tabs>